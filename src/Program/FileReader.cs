using System.IO;

namespace Ucu.Poo.GameOfLife;

public class FileReader
{
/* esto es no mas un borrador de la logica... perdon q lo suba a main
for (int y = 0; y < height; y++)
{
   string line = lines[y]

   for (int x = 0; x < width; x++)
       
}
}